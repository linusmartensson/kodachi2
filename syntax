# <- start new panel in current tier
! <- add a caption
* <- add list item
(text)avatar_name) <- speech bubble
@(url) <- image
_ <- start new tier
| <- start new page
newline starts text element in current panel

